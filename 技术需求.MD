# JAVA服务器代码热更新方案

## 一、项目概述

做一个服务器热更新工具，支持增加新类、旧类只修改方法内的逻辑，是一个使用 ClassLoader + Instrumentation 的热更新方案。

- **项目名称**：hotfix-tools
- **JDK版本**：JDK 8（1.8.0_201）
- **构建工具**：Maven

### 模块划分

| 模块             | 职责                                                       | 稳定性       |
| ---------------- | ---------------------------------------------------------- | ------------ |
| hotfix-cli       | Attach 到目标 JVM，加载 agent                              | 永不变       |
| hotfix-agent     | 自动区分新老类、加载新类、redefine 旧类                    | 永不变       |
| hotfix-patch.jar | 业务热更新内容（新增类 + 修改后的旧类，打在同一个 jar 里） | 每次热更都换 |

### 配置文件

无。patch.jar 路径直接作为 hotfix-cli 的第 3 个命令行参数传入，不再需要 hotfix-config.json。

---

## 二、业务场景

1. **绝大多数情况下**：只修改旧类的方法内逻辑，不改类结构（不加方法、不加字段、不改签名）。
2. **少部分情况下**：需要新增类，然后旧类的方法内逻辑调用新增类的静态方法、或者 `new NewClass()` 后调用成员方法。

---

## 三、技术原理

### 3.1 Attach API（hotfix-cli 的工作原理）

hotfix-cli 是一个独立 JVM 进程，通过 Attach API 连接到目标 GameServer JVM。

**Linux 上的完整流程**：

```
Attaching JVM (hotfix-cli)              Target JVM (GameServer, PID=X)
─────────────────────────              ──────────────────────────────────
1. VirtualMachine.attach(pid)
2. 检查 /tmp/.java_pid<pid> 是否存在 → 不存在
3. 创建 /tmp/.attach_pid<pid> 文件
4. 发送 SIGQUIT (kill -3) ──────────────→  5. Signal Dispatcher 线程接收
                                           6. 检查 /tmp/.attach_pid<pid> → 存在
                                           7. 创建 Unix socket /tmp/.java_pid<pid>
                                           8. 启动 Attach Listener 线程监听
9. 轮询检测到 socket 文件出现 ←──────────
10. 通过 Unix socket 连接
11. 发送 load 命令 + agent.jar 路径 ─────→  12. 加载 agent jar, 创建 ClassLoader
                                           13. 调用 agentmain(args, inst)
14. 收到响应, detach() ←──────────────────
```

**关键点**：

- 两个 JVM 通过 Unix Domain Socket 通信
- SIGQUIT 信号触发 Target JVM 创建 socket 文件
- Attach API 要求两个进程是同一个用户（或 root）
- JVM 启动参数不能带 `-XX:+DisableAttachMechanism`

### 3.2 自动区分新老类

Agent 收到 patch.jar 后，扫描其中的所有 .class 文件，用 `Instrumentation.getAllLoadedClasses()` 判断每个类是新还是老：

- 在 JVM 已加载类集合中 → **旧类**，用 `redefineClasses` 更新方法体
- 不在已加载类集合中 → **新类**，由 AppClassLoader 加载

```java
// 拿到 JVM 当前已加载的所有类名
Set<String> loadedNames = new HashSet<>();
for (Class<?> c : inst.getAllLoadedClasses()) {
    loadedNames.add(c.getName());
}

for (String className : jarClassNames) {
    if (loadedNames.contains(className)) {
        oldClasses.add(className);   // → redefineClasses
    } else {
        newClasses.add(className);   // → AppClassLoader 加载
    }
}
```

**为什么用 `getAllLoadedClasses()` 而不是 `Class.forName`**：

- `getAllLoadedClasses()` 是纯查询，不触发任何类加载行为
- `Class.forName` 会触发类加载/初始化，可能产生副作用（如执行 static 块）
- `getAllLoadedClasses()` 语义清晰：已加载 = 旧类，未加载 = 新类

**前提假设**：本方案假定业务类均由 **AppClassLoader** 加载。`getAllLoadedClasses()` 返回的是**所有 ClassLoader** 已加载的类，而后续 step 用 `Class.forName(name, false, ClassLoader.getSystemClassLoader())` 拿 Class 对象去 redefine——只作用于 AppClassLoader 的那份。若某类被**自定义 ClassLoader** 加载而 AppClassLoader 未加载，会被判为"旧类"但 redefine 作用于 AppClassLoader 新加载的副本，原 loader 的那份不受影响（类身份分裂），热更对该类实际失效。被自定义 ClassLoader 加载的类不在本方案覆盖范围内。

### 3.3 旧类热更：redefineClasses

`Instrumentation.redefineClasses(ClassDefinition...)` 直接替换已加载类的字节码。

**JVMTI 规范限制——只能改方法体，不能改类结构**：

| 操作                   | redefine 是否允许 |
| ---------------------- | ----------------- |
| 改已有方法的方法体逻辑 | ✅                |
| 改常量池               | ✅                |
| 新增静态方法           | ❌                |
| 新增实例方法           | ❌                |
| 新增字段               | ❌                |
| 改方法签名             | ❌                |
| 改继承关系             | ❌                |

如果尝试 redefine 一个加了方法的 .class，JVM 会拒绝并报错：

> 注：表中"改常量池 ✅"指修改方法体内引用的常量值（如字符串字面量、数字常量）等不影响类 schema 的常量池变更；**不能**通过常量池变更引入新方法/字段/改签名——这些仍属于 schema 变更，会被拒绝。

```
attempted to add a method
java.lang.UnsupportedOperationException: class redefinition failed: attempted to add a method
```

**redefineClasses vs retransformClasses**：

|                  | redefineClasses                       | retransformClasses                        |
| ---------------- | ------------------------------------- | ----------------------------------------- |
| 提供字节码方式   | 直接传`ClassDefinition(clz, bytes)` | 注册 Transformer，JVM 回调`transform()` |
| 需要 Transformer | 不需要                                | 需要                                      |
| 适合场景         | 已有预编译 bytes                      | 需要动态生成/修改 bytes                   |
| schema 限制      | 相同（不能加方法/字段）               | 相同                                      |

本方案已有预编译好的 .class 文件（在 patch.jar 里），`redefineClasses` 直接喂进去，不需要 Transformer，代码更简单。

**线程安全**：redefine 是一个 VM_Operation，在 JVM safepoint 完成类元数据替换，**不等**正在执行该方法体的线程退出。正在被 redefine 方法栈帧内执行的线程会继续跑**旧字节码**（HotSpot 用 previous version 元数据保活旧方法体），直到方法返回后才用新逻辑——**JVM 不做栈上替换（OSR）**。因此 `while(true)` 死循环方法 redefine 后虽"成功"，但运行中的线程永远拿不到新逻辑。另：若某线程长时间不触达 safepoint 轮询点（如 JIT 把循环的 safepoint poll 优化掉），会推迟 redefine 的执行时机。

### 3.4 新类加载：appendToSystemClassLoaderSearch + AppClassLoader

`Instrumentation.appendToSystemClassLoaderSearch(JarFile)` 把 patch.jar 追加到 AppClassLoader 的搜索路径。追加后，AppClassLoader 能找到新类。

```java
inst.appendToSystemClassLoaderSearch(new JarFile(patchJarPath));
// 之后 AppClassLoader 可加载 patch.jar 中的新类
```

### 3.5 核心问题：旧类方法体能否直接 new 新类

**能。** 新类和旧类都在 AppClassLoader 中，旧类 redefined 后的方法体里直接 `new NewClass()` 或调用 `NewClass.staticMethod()` 都没问题。

原因：JVM 解析字节码中的类引用时，使用包含该字节码的类的 defining ClassLoader 来解析。旧类由 AppClassLoader 加载，`new NewClass()` 走 AppClassLoader 查找，新类也在 AppClassLoader（通过 appendToSystemClassLoaderSearch 加入），能找到。

**限制**：新类被 AppClassLoader 加载后**永远无法卸载**，不能反复热更同一个新类。适合"一次性追加新类"的场景。如果将来需要反复热更同一个新类，再扩展 Registry + 接口方案。

---

## 四、架构设计

```
┌─── hotfix-cli JVM (独立进程) ──────────────────────────────────┐
│  java -jar hotfix-cli.jar <pid> <agent.jar> <patch.jar>         │
│  VirtualMachine.attach(pid)                                     │
│  vm.loadAgent(agentJar, patchJarAbsPath)                        │
└───────────────────────────┬─────────────────────────────────────┘
                            │ Attach API (Unix Socket)
                            ▼
┌─── Target JVM (GameServer 进程) ──────────────────────────────────┐
│                                                                   │
│  ┌─── Agent (agentmain 入口) ──────────────────────────────────┐  │
│  │  1. 接收 cli 传来的 patch.jar 绝对路径（agentArgs）           │  │
│  │  2. 扫描 patch.jar 中所有 .class 文件                        │  │
│  │  3. 用 getAllLoadedClasses() 区分新老类                       │  │
│  │  4. appendToSystemClassLoaderSearch(patch.jar)              │  │
│  │  5. 新类: AppClassLoader 自动加载 (或显式预加载)              │  │
│  │  6. 旧类: 从 jar 读 bytes, redefineClasses 直接替换          │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌─── AppClassLoader ──────────────────────────────────────────┐  │
│  │  classpath 追加了 patch.jar                                   │  │
│  │  能找到新类 (NewHelper, BattleHelper 等)                      │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌─── Old Classes (已加载, redefine 后) ───────────────────────┐  │
│  │  SkillModule (方法体已替换为调用新逻辑)                       │  │
│  │  → 方法体内可以直接 new NewClass()                            │  │
│  │  → 方法体内可以直接调用 NewHelper.staticMethod()              │  │
│  └──────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────┘
```

---

## 五、JVM 参数要求

### 5.1 目标服务器进程（GameServer）

**JDK 8 下几乎不需要额外参数**，Attach 机制和 Instrumentation 默认可用。

必须确保：

```bash
# 不能有这个参数（加了就无法 attach）:
# -XX:+DisableAttachMechanism
```

建议加上（方便排查）：

```bash
-XX:+TraceClassLoading
-XX:+TraceClassUnloading
```

Metaspace 给大点（每次热更会占用新的 Metaspace 空间）：

```bash
-XX:MaxMetaspaceSize=512m
```

### 5.2 运行时权限要求

- hotfix-cli.jar 运行的用户必须和 GameServer 进程**同一个用户**（或 root）
- `/tmp` 目录可写（Unix socket 文件的位置）
- hotfix-cli 需要 `com.sun.tools.attach.VirtualMachine`（JDK8 在 `$JAVA_HOME/lib/tools.jar`）。本工具已用 maven-shade-plugin 把 attach API 类内嵌进 cli jar，运行时自包含；未 shade 的版本需 JDK 运行或 `-cp tools.jar`
- GameServer 目标进程**不要求 JDK**，JRE 即可——attach 监听由 JRE 自带的原生库（`libattach.so`/`attach.dll`）提供，不需要 tools.jar

---

## 六、开发步骤

### 6.1 Step 1: hotfix-cli —— Attach 入口

```java
// HotFixMain.java
import com.sun.tools.attach.VirtualMachine;

public class HotFixMain {
    public static void main(String[] args) throws Exception {
        // args: <pid> <agent.jar> <patch.jar>
        String pid = args[0];
        String agentJar = args[1];
        String patchJar = args.length > 2 ? args[2] : "";

        // 1. Attach 到目标 JVM
        VirtualMachine vm = VirtualMachine.attach(pid);

        // 2. 加载 agent jar 到目标 JVM 内
        //    patch.jar 路径会作为 agentArgs 传给 agentmain()
        vm.loadAgent(agentJar, patchJar);

        // 3. 断开连接
        vm.detach();

        System.out.println("Hotfix applied successfully.");
    }
}
```

**编译与运行注意**：JDK 8 下 `com.sun.tools.attach.VirtualMachine` 在 `$JAVA_HOME/lib/tools.jar`。

为了让 `java -jar hotfix-cli.jar` 在 JDK8 下直接可用（无需 `-cp tools.jar`），工程用 maven-shade-plugin 把 tools.jar 的 attach API 类内嵌进 cli jar。shade 不会打包 `system` scope 依赖，因此需先把 tools.jar 装入本地 Maven 仓库再用 `compile` scope：

```bash
# 每台构建机各执行一次
mvn install:install-file -Dfile=$JAVA_HOME/lib/tools.jar \
    -DgroupId=com.sun -DartifactId=tools -Dversion=1.8 -Dpackaging=jar -DgeneratePom=true
```

```xml
<!-- pom.xml 依赖（compile scope，才会被 shade 打包） -->
<dependency>
    <groupId>com.sun</groupId>
    <artifactId>tools</artifactId>
    <version>1.8</version>
    <scope>compile</scope>
</dependency>
```

shade 配置需过滤签名文件（`META-INF/*.SF|*.DSA|*.RSA`）并保留 `META-INF/services`（AttachProvider 服务发现），详见工程 `hotfix-cli/pom.xml`。

### 6.2 Step 2: hotfix-agent —— Agent 核心

```java
// HotFixAgent.java
import java.lang.instrument.Instrumentation;
import java.lang.instrument.ClassDefinition;
import java.io.File;
import java.io.IOException;
import java.io.InputStream;
import java.io.FileNotFoundException;
import java.util.*;
import java.util.jar.*;

public class HotFixAgent {

    // 通过 vm.loadAgent() 注入后, JVM 会调用这个方法
    public static void agentmain(String agentArgs, Instrumentation inst)
            throws Exception {

        // agentArgs 是 CLI 传过来的 patch.jar 路径（绝对路径）
        File patchJar = new File(agentArgs);

        System.out.println("=== Hotfix Agent Start ===");
        System.out.println("Patch jar: " + patchJar.getAbsolutePath());

        // 1. 扫描 jar 中所有 class 文件, 拿到全限定类名列表
        List<String> allClassNames = scanJarClassNames(patchJar);
        System.out.println("Found " + allClassNames.size() + " classes in jar.");

        // 2. 用 getAllLoadedClasses() 区分新老类
        Set<String> loadedNames = new HashSet<>();
        for (Class<?> c : inst.getAllLoadedClasses()) {
            loadedNames.add(c.getName());
        }

        List<String> newClasses = new ArrayList<>();
        List<String> oldClasses = new ArrayList<>();
        for (String name : allClassNames) {
            if (loadedNames.contains(name)) {
                oldClasses.add(name);
            } else {
                newClasses.add(name);
            }
        }

        System.out.println("New classes (" + newClasses.size() + "): " + newClasses);
        System.out.println("Old classes (" + oldClasses.size() + "): " + oldClasses);

        // 3. append patch.jar 到 AppClassLoader 搜索路径
        //    这样 AppClassLoader 就能找到新类了
        inst.appendToSystemClassLoaderSearch(new JarFile(patchJar));
        System.out.println("Appended patch.jar to system classloader.");

        // 4. 新类: 预加载 (可选, 也可以等旧类方法体调用时自动触发)
        //    预加载的好处是能提前发现编译错误
        for (String name : newClasses) {
            try {
                Class.forName(name, true, ClassLoader.getSystemClassLoader());
                System.out.println("  Loaded new class: " + name);
            } catch (Throwable e) {
                System.err.println("  Failed to load new class: " + name + " - " + e);
                throw e;
            }
        }

        // 5. 旧类: 从 jar 读取 bytes, 用 redefineClasses 直接替换
        if (!oldClasses.isEmpty()) {
            List<ClassDefinition> defs = new ArrayList<>();
            for (String name : oldClasses) {
                Class<?> clz = Class.forName(name, false,
                    ClassLoader.getSystemClassLoader());
                byte[] bytes = readClassFromJar(patchJar, name);
                defs.add(new ClassDefinition(clz, bytes));
                System.out.println("  Redefining: " + name 
                    + " (" + bytes.length + " bytes)");
            }
            inst.redefineClasses(defs.toArray(new ClassDefinition[0]));
            System.out.println("Redefine done for " + defs.size() + " classes.");
        }

        System.out.println("=== Hotfix Agent Done ===");
    }

    // 扫描 jar 中所有 .class 文件, 返回全限定类名列表
    private static List<String> scanJarClassNames(File jarFile) throws IOException {
        List<String> names = new ArrayList<>();
        try (JarFile jar = new JarFile(jarFile)) {
            Enumeration<JarEntry> entries = jar.entries();
            while (entries.hasMoreElements()) {
                JarEntry entry = entries.nextElement();
                String name = entry.getName();
                if (name.endsWith(".class") && !name.contains("module-info")) {
                    String className = name.substring(0, name.length() - 6)
                        .replace('/', '.');
                    names.add(className);
                }
            }
        }
        return names;
    }

    // 从 jar 中读取指定 class 的字节码
    private static byte[] readClassFromJar(File jarFile, String className)
            throws IOException {
        String entryName = className.replace('.', '/') + ".class";
        try (JarFile jar = new JarFile(jarFile)) {
            JarEntry entry = jar.getJarEntry(entryName);
            if (entry == null) {
                throw new FileNotFoundException("Class not found in jar: " + entryName);
            }
            try (InputStream is = jar.getInputStream(entry)) {
                java.io.ByteArrayOutputStream bos = new java.io.ByteArrayOutputStream();
                byte[] buf = new byte[8192];
                int n;
                while ((n = is.read(buf)) != -1) {
                    bos.write(buf, 0, n);
                }
                return bos.toByteArray();
            }
        }
    }
}
```

**MANIFEST.MF**（打在 agent jar 里）：

```
Manifest-Version: 1.0
Agent-Class: HotFixAgent
Can-Redefine-Classes: true
Can-Retransform-Classes: true
Can-Set-Native-Method-Prefix: false
```

### 6.3 Step 3: 命令行参数（无配置文件）

patch.jar 路径直接作为 hotfix-cli 的第 3 个命令行参数传入，不再需要 hotfix-config.json：

```
java -jar hotfix-cli.jar <pid> <agent.jar> <patch.jar>
```

| 参数 | 说明 |
| ---- | ---- |
| pid | 目标 GameServer JVM 进程 PID |
| agent.jar | hotfix-agent.jar 路径 |
| patch.jar | 热更 jar 包路径，包含新增类和修改后的旧类（相对路径以 cli CWD 为基准） |

cli 把 patch.jar 路径解析为绝对路径后作为 agentArgs 传给 agent。新老类的区分由 Agent 自动完成，不需要手动配置哪些是新类哪些是旧类。

---

## 七、使用流程

### 7.1 开发机上

```
1. 编写新增类
   例: NewSkillHelper.java (包含新的静态方法)

2. 修改旧类源码 (只改方法体逻辑, 不加方法不加字段)
   例: SkillModule.java
       原方法体: int dmg = req.getBaseDamage() * 2;
       新方法体: int dmg = NewSkillHelper.calcDamage(req);  // 调用新类的静态方法

3. 编译所有类:
   javac -d /tmp/classes NewSkillHelper.java SkillModule.java

4. 把所有编译产物打进一个 jar:
   jar cf hotfix-patch.jar -C /tmp/classes .

5. 拷贝 hotfix-patch.jar 到服务器
6. 执行 hotfix-cli 时把 patch.jar 路径作为第 3 个参数传入（见 7.2）
```

### 7.2 服务器上

```
/opt/gameserver/
├── GameServer.jar              ← 游戏服务器主程序 (运行中)
├── hotfix/
│   ├── hotfix-cli.jar          ← 工具
│   ├── hotfix-agent.jar        ← 工具
│   └── hotfix-patch.jar        ← 本次热更内容 (新类 + 旧类都在里面)
└── logs/

执行热更:
  pid=$(jps | grep GameServer | awk '{print $1}')
  cd /opt/gameserver/hotfix
  java -jar hotfix-cli.jar $pid hotfix-agent.jar hotfix-patch.jar
```

### 7.3 执行时序

```
1. hotfix-cli 启动, attach 到 GameServer PID
2. loadAgent 加载 hotfix-agent.jar
3. agentmain 执行:
   a. 扫描 patch.jar, 拿到所有 class 名
   b. getAllLoadedClasses() 区分新老:
      - 已加载 → 旧类
      - 未加载 → 新类
   c. appendToSystemClassLoaderSearch(patch.jar)
   d. 新类: Class.forName 预加载 (走 AppClassLoader)
   e. 旧类: 从 jar 读 bytes → redefineClasses
4. redefine 在 safepoint 执行, 替换方法体字节码
5. 之后 SkillModule.handle() 的方法体已是新逻辑, 直接调用 NewSkillHelper
6. hotfix-cli detach, 退出
```

### 7.4 Agent 输出示例

```
=== Hotfix Agent Start ===
Patch jar: /opt/gameserver/hotfix/hotfix-patch.jar
Found 3 classes in jar.
New classes (1): [com.zulong.sa.skill.NewSkillHelper]
Old classes (2): [com.zulong.sa.skill.SkillModule, com.zulong.sa.battle.BattleLogic]
Appended patch.jar to system classloader.
  Loaded new class: com.zulong.sa.skill.NewSkillHelper
  Redefining: com.zulong.sa.skill.SkillModule (3456 bytes)
  Redefining: com.zulong.sa.battle.BattleLogic (2890 bytes)
Redefine done for 2 classes.
=== Hotfix Agent Done ===
```

---

## 八、约束与限制

### 8.1 旧类 redefine 的硬限制

- ✅ 能改已有方法的方法体逻辑
- ❌ 不能加方法（静态/实例都不行）
- ❌ 不能加字段
- ❌ 不能改方法签名
- ❌ 不能改继承关系

如果需要加方法/字段/改签名，必须放到新增类里，旧类方法体调用它。

### 8.2 新增类的限制

- 新增类被 AppClassLoader 加载后**无法卸载**，不能反复热更同一个新类
- 新增类可以加方法、加字段、改签名，无任何限制
- 新增类和旧类在同一个 ClassLoader（AppClassLoader），无类身份分裂问题

### 8.3 自动区分新老类的边界情况

- 正常情况：旧类已在 JVM 中加载，新类未加载，`getAllLoadedClasses()` 能准确区分
- 边界情况：某个类在服务器 classpath 上存在但尚未被加载，且 patch.jar 中也有同名的修改版 → 会被识别为"新类" → AppClassLoader 加载的是服务器 classpath 上的旧版本而非 patch.jar 中的新版本
  - 规避：不要在 patch.jar 中放与服务器 classpath 上同名但未加载的类
- 边界情况：某新类在第一次热更后已被 AppClassLoader 加载，第二次热更想更新它 → 会被识别为"旧类" → 尝试 redefine，如果新版本加了方法会失败
  - 规避：新类设计为只增不改；如果需要改，只能改方法体（不加方法），才会 redefine 成功

### 8.4 线程安全

- redefine 是 VM_Operation，在 safepoint 完成类元数据替换
- **不等**正在执行该方法体的线程退出；运行中的栈帧继续跑旧字节码，方法返回后才用新逻辑（JVM 无 OSR）
- `while(true)` 死循环方法 redefine 后"成功"，但运行中的线程永远拿不到新逻辑
- 若线程长时间不触达 safepoint 轮询点（如 JIT 把循环 safepoint poll 优化掉），会推迟 redefine 执行时机
- **游戏服务端 main loop 不能直接 redefine**（main loop 不返回，新逻辑永远不生效）

### 8.5 Metaspace

- 每次热更新增类会占用新的 Metaspace 空间（不可回收）
- 旧类 redefine 会**瞬时**分配新版本元数据（Method/ConstMethod/ConstantPool 等），旧版本作为 previous version 保留，待运行中的旧栈帧全部退出后回收——所以**短期内 Metaspace 会增长**，不是零成本
- 若被 redefine 的方法有长期不退出的栈帧（如 main loop），旧版本不释放，多次热更会累积
- 建议设置 `-XX:MaxMetaspaceSize=512m` 或更大

### 8.6 多次热更

- 旧类可以反复 redefine（每次只改方法体）
- 新增类一旦加载就无法替换（AppClassLoader 不卸载类）
- 新增类可以被后续热更的旧类方法体引用
- 如果新增类需要反复热更，需改用 Registry + 接口 + 独立 ClassLoader 方案（后续扩展）

---

## 九、风险与规避

| 风险                                       | 严重程度 | 规避方法                                                      |
| ------------------------------------------ | -------- | ------------------------------------------------------------- |
| redefine 改了类结构导致失败                | 高       | 编译前确认没有加方法/字段, 只改方法体                         |
| 长循环方法 redefine 阻塞                   | 中       | 避免热更 main loop 类, 检查目标方法是否含死循环               |
| Metaspace OOM                              | 中       | 设置足够大的 MaxMetaspaceSize                                 |
| 权限不足导致 attach 失败                   | 高       | 确保 hotfix-cli 和 GameServer 同用户运行                      |
| 新增类编译版本不匹配                       | 中       | 编译新增类时用和服务器相同的 JDK 版本                         |
| appendToSystemClassLoaderSearch 找不到 jar | 高       | cli 参数用绝对路径，或 cd 进 jar 所在目录用相对路径           |
| 同名未加载类被误判为新类                   | 低       | 不在 patch.jar 中放与服务器 classpath 同名但未加载的类        |
| 新类二次热更被误判为旧类                   | 中       | 新类设计为只增不改；如需改方法体, redefine 可以, 但不能加方法 |
| agentArgs 含非 ASCII 路径乱码             | 低/高    | patchJar 路径用 ASCII；含中文路径时拷到 ASCII 目录再跑（生产路径天然 ASCII） |

---

## 十、后续扩展方向

如果将来需要"反复热更同一个新类"，可以在当前架构基础上扩展 Registry + 接口方案：

1. Agent 用独立的 HotFixClassLoader 加载新类
2. 新类实现已有接口（接口由 AppClassLoader 加载，不参与热更）
3. Agent 实例化新类后注册到 HotFixRegistry
4. 旧类方法体通过 Registry 获取实例，通过接口调用
5. HotFixClassLoader 可卸载，实现新类替换

此方案代码侵入性更高（需要接口和 Registry），但支持新类的反复热更。
